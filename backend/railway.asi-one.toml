[build]
builder = "dockerfile"

[deploy]
startCommand = "uv run python run_asi_one_chat_agent.py"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
